<style>
    .loading.loading-mask {
        height: 100vh;
        width: 100vw;
        position: fixed;
        background: rgba(0, 0, 0, 0.6);
        display: flex;
        /* align-items: center;
        justify-content: center; */
        z-index: 999;
    }

    .loading.loading-mask .loading-span {
        display: inline-block;
        font-size: 4vw;
        line-height: 5vw;
        height: 5vw;
        color: #fff;
        position: absolute;
        top: 50%;
        left: 50%;
        margin-top: 2.5vw;
        margin-left: -10vw;
        overflow: hidden;
        width: 16vw;
        text-overflow: clip;
        white-space: nowrap;
        animation: breakWord 1.5s linear infinite;
    }

    @keyframes breakWord {
        0% {
            width: 16vw;
        }

        24% {
            width: 16vw;
        }

        25% {
            width: 20vw;
        }

        49% {
            width: 20vw;
        }

        50% {
            width: 24vw;
        }

        74% {
            width: 24vw;
        }

        75% {
            width: 28vw;
        }
        99% {
            width: 28vw;
        }
        100% {
            width: 16vw;
        }
    }
</style>
<div class="loading loading-mask">
    <div class="loading-span">
        <span>加载中 。。。。。</span>
    </div>
</div>